export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';

// API instance configuration could go here if using axios or similar
// For now, we'll just export the URL for services to use
